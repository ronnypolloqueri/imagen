<%= form_for(@cliente, :html => { :class => "form-horizontal"}) do |f| %>
  <% if @cliente.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cliente.errors.count, "error") %> prohibited this cliente from being saved:</h2>
      <ul>
      <% @cliente.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="control-group">
    <%= f.label(:nombres, :class=>'control-label') %>
    <div class="controls">
      <%= f.text_field :nombres, :class=>'input-xxlarge'%>
    </div>
  </div>
  <div class="control-group">
    <%= f.label(:apellido_paterno, :class=>'control-label') %>
    <div class="controls">
      <%= f.text_field :apellido_paterno, :class=>'input-large'%>
    </div>
  </div>
  <div class="control-group">
    <%= f.label(:apellido_materno, :class=>'control-label') %>
    <div class="controls">
      <%= f.text_field :apellido_materno, :class=>'input-large'%>
    </div>
  </div>
  <div class="control-group">
    <%= f.label(:telefonos, 'Teléfonos', :class=>'control-label') %>
    <div class="controls">
      <%= f.text_field :telefonos, :class=>'input-xlarge'%>
    </div>
  </div>
  <div class="control-group">
    <%= f.label(:correo_electronico, 'Correo electrónico', :class=>'control-label') %>
    <div class="controls">
      <%= f.text_field :correo_electronico, :class=>'input-xlarge'%>
    </div>
  </div>
  <div class="control-group">
    <%= f.label(:direccion, 'Dirección', :class=>'control-label') %>
    <div class="controls">
      <%= f.text_field :direccion, :class=>'input-xxlarge'%>
    </div>
  </div>
  <div class="form-actions">
    <%= f.submit 'Guardar', :class=>'btn btn-primary' %>
  </div>  
<% end %>
